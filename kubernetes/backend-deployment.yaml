apiVersion: apps/v1
kind: Deployment
metadata:
  name: expensy-backend
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: expensy-backend
  template:
    metadata:
      labels:
        app: expensy-backend
    spec:
      containers:
        - name: backend
          # REPLACE 'yourusername' with your actual Docker Hub profile username
          image: ahsan4321/expensy-backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8706
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: expensy-config
                  key: PORT
            - name: DATABASE_URI
              valueFrom:
                secretKeyRef:
                  name: expensy-secrets
                  key: DATABASE_URI
            - name: REDIS_HOST
              valueFrom:
                configMapKeyRef:
                  name: expensy-config
                  key: REDIS_HOST
            - name: REDIS_PORT
              valueFrom:
                configMapKeyRef:
                  name: expensy-config
                  key: REDIS_PORT
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: expensy-secrets
                  key: REDIS_PASSWORD
